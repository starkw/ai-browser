// Minimal ambient module declarations to satisfy TypeScript on Render
// We don't need full typings for these modules in the server-only upload route
declare module "pdf-parse";
declare module "mammoth";

